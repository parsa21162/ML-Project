# 📊 پوشه داده‌ها (Data Directory)

## 📁 ساختار

```
data/
├── raw/                          # داده‌های خام
│   ├── product_reviews_2000.csv  # نظرات محصول (2 کلاس)
│   ├── news_classification_2000.csv  # اخبار (4 کلاس)
│   └── [your_dataset.csv]        # دیتاست شما
│
└── processed/                    # داده‌های پردازش‌شده
    └── (خودکار ایجاد می‌شود)
```

---

## 🎯 دیتاست‌های موجود

### 1️⃣ Product Reviews (نظرات محصول)
**فایل:** `product_reviews_2000.csv`

- **تعداد نمونه:** 2000
- **کلاس‌ها:** 2 (Positive=1, Negative=0)
- **توازن:** 50% هر کلاس
- **موضوع:** نظرات مشتریان درباره محصولات

**ستون‌ها:**
- `id`: شناسه یکتا
- `text`: متن نظر
- `label`: برچسب (0 یا 1)

**مثال:**
```csv
id,text,label
1,"This product is amazing! Love it!",1
2,"Poor quality. Not recommended.",0
```

**نحوه استفاده:**
```bash
python main.py --data data/raw/product_reviews_2000.csv \
    --text_col text --label_col label --model baseline
```

---

### 2️⃣ News Classification (طبقه‌بندی اخبار)
**فایل:** `news_classification_2000.csv`

- **تعداد نمونه:** 2000
- **کلاس‌ها:** 4 (Technology, Sports, Business, Entertainment)
- **توازن:** 500 نمونه هر کلاس
- **موضوع:** دسته‌بندی اخبار

**ستون‌ها:**
- `id`: شناسه یکتا
- `text`: متن خبر
- `label`: برچسب عددی (0-3)
- `category`: نام دسته

**مثال:**
```csv
id,text,label,category
1,"Apple announces new iPhone...",0,Technology
2,"Local team wins championship...",1,Sports
```

**نحوه استفاده:**
```bash
python main.py --data data/raw/news_classification_2000.csv \
    --text_col text --label_col label --model cnn
```

---

## 📥 دانلود دیتاست‌های بزرگ‌تر

برای پروژه‌های جدی‌تر، از این دیتاست‌ها استفاده کنید:

### 1. IMDB Movie Reviews (50K)
```bash
# از Kaggle دانلود کنید
https://www.kaggle.com/datasets/lakshmi25npathi/imdb-dataset-of-50k-movie-reviews
```

### 2. AG News (120K)
```bash
https://www.kaggle.com/datasets/amananandrai/ag-news-classification-dataset
```

### 3. Twitter Sentiment
```bash
https://www.kaggle.com/datasets/jp797498e/twitter-entity-sentiment-analysis
```

### 4. Amazon Reviews
```bash
https://www.kaggle.com/datasets/bittlingmayer/amazonreviews
```

---

## 🔧 ساخت دیتاست خودتان

### از اسکریپت‌های موجود:

```bash
# دیتاست نظرات محصول
python create_realistic_dataset.py

# دیتاست اخبار چندکلاسه
python create_news_dataset.py
```

### فرمت مورد نیاز:

دیتاست شما باید **CSV** باشد با حداقل دو ستون:

```csv
text,label
"Your text here",0
"Another text",1
```

**نکات:**
- ستون متن می‌تواند هر نامی داشته باشد (text, review, comment, ...)
- ستون برچسب باید **عددی** باشد (0, 1, 2, ...)
- از encoding UTF-8 استفاده کنید
- حداقل 500 نمونه توصیه می‌شود

---

## 📊 آمار دیتاست‌ها

| دیتاست | نمونه‌ها | کلاس‌ها | توازن | متوسط کلمات |
|--------|----------|---------|--------|-------------|
| Product Reviews | 2000 | 2 | متوازن | ~15 |
| News Classification | 2000 | 4 | متوازن | ~12 |

---

## 🚀 شروع سریع

```bash
# 1. مشاهده دیتاست
cd data/raw
head -20 product_reviews_2000.csv

# 2. اجرای EDA
jupyter notebook ../../notebooks/01_EDA.ipynb

# 3. آموزش مدل
cd ../..
python main.py --data data/raw/product_reviews_2000.csv --model baseline

# 4. آموزش مدل عمیق
python main.py --data data/raw/news_classification_2000.csv --model cnn --epochs 10
```

---

## ⚠️ نکات مهم

### حجم داده
- **حداقل:** 500 نمونه
- **توصیه شده:** 1000-5000 نمونه
- **ایده‌آل:** 10000+ نمونه

### توازن کلاس‌ها
- بهترین حالت: کلاس‌ها یکسان باشند
- قابل قبول: نسبت 1:2 یا بهتر
- مشکل‌دار: نسبت 1:5 یا بدتر (نیاز به class balancing)

### کیفیت داده
- ✅ متن‌های تمیز و خوانا
- ✅ برچسب‌های صحیح
- ✅ بدون داده تکراری زیاد
- ✅ بدون مقادیر خالی (NaN)

---

## 🔍 بررسی کیفیت داده

```python
import pandas as pd

# بارگذاری داده
df = pd.read_csv('data/raw/your_dataset.csv')

# بررسی پایه
print("Shape:", df.shape)
print("\nColumns:", df.columns.tolist())
print("\nNull values:", df.isnull().sum())
print("\nDuplicates:", df.duplicated().sum())

# بررسی کلاس‌ها
print("\nClass distribution:")
print(df['label'].value_counts())

# بررسی طول متن
df['length'] = df['text'].str.len()
print("\nText length statistics:")
print(df['length'].describe())
```

---

## 💡 پیشنهادات

1. **برای پروژه آموزشی:** از دیتاست‌های موجود استفاده کنید
2. **برای پروژه واقعی:** دیتاست بزرگ‌تر از Kaggle دانلود کنید
3. **برای تحقیق:** دیتاست خودتان را جمع‌آوری کنید

---

## 📞 سوالات متداول

**Q: چطور داده بیشتر بسازم؟**
A: اسکریپت‌های `create_*.py` را ویرایش کنید و تعداد نمونه‌ها را افزایش دهید.

**Q: دیتاست فارسی دارید؟**
A: می‌توانید از دیتاست‌های فارسی مثل Digikala reviews استفاده کنید.

**Q: چطور داده خودم را اضافه کنم؟**
A: فایل CSV با فرمت مشخص شده بسازید و در `data/raw/` قرار دهید.

---

**موفق باشید! 📊**
